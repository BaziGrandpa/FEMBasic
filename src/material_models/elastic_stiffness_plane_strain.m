function D = elastic_stiffness_plane_strain(E, nu)
% D = elastic_stiffness_plane_strain(E, nu)
%   Calculate the isotropic elastic tangent stiffness, `D`, in Voigt 
%   notation for a plane strain stress state. 
 D = eye(3,3);
end
