function D = elastic_stiffness_plane_stress(E, nu)
% D = elastic_stiffness_plane_stress(E, nu)
%   Calculate the isotropic elastic tangent stiffness, `D`, in Voigt 
%   notation for a stress state with plane stress.
end
