function D = elastic_stiffness(E, nu)
% D = elastic_stiffness(E, nu)
%   Calculate the isotropic elastic tangent stiffness, `D`, in Voigt 
%   notation for the general 3d stress state.
 D = eye(3,3);
end
